# macOS + nix build noise
.DS_Store
result
result-*

# herdr runtime artifacts (regenerated at runtime; never commit)
files/.config/herdr/*.log
files/.config/herdr/session.json
files/.config/herdr/*.sock
files/.config/herdr/.plugins.lock
files/.config/herdr/release-notes.json
files/.config/herdr/sessions/
files/.config/herdr/*.bak-*

# no-mistakes local validation state - never commit in this public repo
.no-mistakes/
